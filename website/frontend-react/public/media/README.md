Placeholder location for the mobile Acts 1-3 fallback video referenced by
`src/components/mobile/MobileHeroFallback.jsx` (`VIDEO_SRC`). Drop the
real export here as `vanguard-transformation-placeholder.mp4` (or update
`VIDEO_SRC` to whatever filename you use).
